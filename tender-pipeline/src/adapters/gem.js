// GeM adapter — placeholder with the same interface as the others.
// GeM mirrors CPPP tender data; wire real extraction/selectors before enabling in config.
export async function fetchRaw(_source, _opts) {
  return [];
}
